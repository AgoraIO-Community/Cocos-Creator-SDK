{
  "name": "agora-cocos-creator-sdk",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc -d --emitDeclarationOnly --outDir ../ && tsc --removeComments --outDir ../agora",
    "doc": "typedoc -p pub-release"
  },
  "devDependencies": {
    "typedoc": "^0.19.2",
    "typescript": "^4.0.5"
  }
}
